#服务配置
server:
  port: 8090
#数据源配置
spring:
  datasource:
    username: root
    password: yqh123
    url: jdbc:mysql://localhost:3306/auth?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    prefix: /WEB-INF/
    suffix: .html
#mybatis配置
mybatis:
  mapper-locations: classpath*:mapper/*.xml



